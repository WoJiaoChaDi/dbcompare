<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chadi.dbcompare.dao.User_proceduresMapper">

    <!--返回List-->
    <select id="getUser_proceduresByPros"  parameterType="java.util.Map" resultType="com.chadi.dbcompare.bean.User_procedures">
		SELECT t1.* FROM User_procedures t1
        where 1=1

        <foreach collection="params.keys" item="key" open="and" close="" separator="and" >
            t1.${key} = #{params[${key}]}
        </foreach>

    </select>

</mapper>